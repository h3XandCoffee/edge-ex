/*
 * LiveDepthPage Messages
 *
 * This contains all the text for the LiveDepthPage component.
 */
import { defineMessages } from 'react-intl';

export const scope = 'boilerplate.containers.LiveDepthPage';

export default defineMessages({
  liveDepthTitle: {
    id: `${scope}.header`,
    defaultMessage: 'Live Book Data',
  },
});
